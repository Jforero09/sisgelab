import { AdminResorucesRegisterComponent } from './admin-resoruces-register.component'

describe('AdminResorucesRegisterComponent', () => {
  it('should mount', () => {
    cy.mount(AdminResorucesRegisterComponent)
  })
})