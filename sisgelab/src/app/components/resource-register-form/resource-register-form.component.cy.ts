import { ResourceRegisterFormComponent } from './resource-register-form.component'

describe('ResourceRegisterFormComponent', () => {
  it('should mount', () => {
    cy.mount(ResourceRegisterFormComponent)
  })
})