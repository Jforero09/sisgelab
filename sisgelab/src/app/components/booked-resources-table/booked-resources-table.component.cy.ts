import { BookedResourcesTableComponent } from './booked-resources-table.component'

describe('BookedResourcesTableComponent', () => {
  it('should mount', () => {
    cy.mount(BookedResourcesTableComponent)
  })
})